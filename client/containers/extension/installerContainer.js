import React, { Component } from 'react';
import Installer from '../../components/extension/installer';

class InstallerContainer extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <Installer />;
  }
}

export default InstallerContainer;