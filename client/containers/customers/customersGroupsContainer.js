import React, { Component } from 'react';
import CustomersGroups from '../../components/customers/CustomersGroups';

class CustomersGroupsContainer extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <CustomersGroups />;
  }
}

export default CustomersGroupsContainer;