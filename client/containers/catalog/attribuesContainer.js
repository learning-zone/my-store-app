import React, { Component } from 'react';
import Attribues from '../../components/catalog/Attribues';

class AttribuesContainer extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <Attribues />;
  }
}

export default AttribuesContainer;