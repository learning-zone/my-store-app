import React, { Component } from 'react';
import ThemeEditor from '../../components/design/themeEditor';

class ThemeEditorContainer extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <ThemeEditor />;
  }
}

export default ThemeEditorContainer;